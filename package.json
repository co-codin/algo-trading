{
  "name": "binance-algo-trading",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "frontend:dev": "vite --host 127.0.0.1",
    "frontend:build": "vue-tsc --noEmit && vite build",
    "frontend:check": "vue-tsc --noEmit && vite build"
  },
  "dependencies": {
    "lightweight-charts": "^5.2.0",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "typescript": "^5.9.3",
    "vite": "^8.0.16",
    "vue-tsc": "^3.1.8"
  }
}
